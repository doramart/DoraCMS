{
  "name": "build",
  "version": "1.0.0",
  "description": "",
  "main": "buildModules.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "installModules": "node ./installModules",
    "buildDevModules": "export NODE_ENV=production && node ./buildModules",
    "buildPrdModules": "export NODE_ENV=production && node ./buildModules --type prd"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "qiniu": "^7.2.2",
    "shelljs": "^0.8.3"
  }
}
