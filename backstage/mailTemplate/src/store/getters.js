const getters = {
  mailTemplateFormState: state => state.mailTemplate.formState,
  mailTemplateList: state => state.mailTemplate.list,
  mailTemplateTypeList: state => state.mailTemplate.typelist,
}
export default getters